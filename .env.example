# SvrCtlRS Configuration Example
# Copy this file to .env and configure for your environment

# ============================================================================
# Server Configuration
# ============================================================================

# Servers to monitor (comma-separated)
# Format: name:ssh_host or just name for local
# Example: SERVERS=webserver:web.example.com,dbserver:db.example.com,localhost
SERVERS=localhost

# SSH Key Path (for remote server access)
# This will be mounted into the container at /app/ssh_key
SSH_KEY_PATH=~/.ssh/id_rsa

# ============================================================================
# Database
# ============================================================================

# Database URL (SQLite by default)
DATABASE_URL=sqlite:/app/data/svrctlrs.db

# ============================================================================
# Notification Backends
# ============================================================================

# Gotify Configuration
GOTIFY_URL=https://gotify.example.com
GOTIFY_KEY=your-gotify-application-key-here

# ntfy.sh Configuration
NTFY_URL=https://ntfy.sh
NTFY_TOPIC=your-topic-name-here

# ============================================================================
# Plugin Configuration
# ============================================================================

# Core Plugins (enabled by default)
ENABLE_DOCKER_PLUGIN=true
ENABLE_UPDATES_PLUGIN=true
ENABLE_HEALTH_PLUGIN=true

# Add-on Plugins (disabled by default)
ENABLE_WEATHER_PLUGIN=false
ENABLE_SPEEDTEST_PLUGIN=false

# Weather Plugin Configuration (if enabled)
OPENWEATHERMAP_API_KEY=your-api-key-here
WEATHER_LOCATION=New York,US
WEATHER_UNITS=imperial

# ============================================================================
# Logging
# ============================================================================

# Log level: trace, debug, info, warn, error
RUST_LOG=info

# ============================================================================
# Server Settings
# ============================================================================

# Server bind address (usually keep as default in Docker)
# BIND_ADDRESS=0.0.0.0:8080

# Webhook token for secure webhook endpoints
# WEBHOOK_TOKEN=your-secure-token-here

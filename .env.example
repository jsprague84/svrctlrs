# SvrCtlRS Docker Compose Environment Variables
# Copy this file to .env and customize for your deployment

# =============================================================================
# Docker Image Configuration
# =============================================================================

# GitHub repository (for pulling images)
GITHUB_REPOSITORY=jsprague84/svrctlrs

# Image tag to use
# - "latest" for stable releases (main branch)
# - "develop" for development builds
# - "v1.0.0" for specific version tags
TAG=latest

# =============================================================================
# Application Configuration
# =============================================================================

# Logging level (trace, debug, info, warn, error)
RUST_LOG=info

# =============================================================================
# Database Configuration
# =============================================================================

# SQLite database path (internal to container)
DATABASE_URL=sqlite:/app/data/svrctlrs.db

# =============================================================================
# Network Configuration
# =============================================================================

# External port to expose the web UI
# Access at: http://localhost:8080
HTTP_PORT=8080

# =============================================================================
# SSH Configuration (for remote server management)
# =============================================================================

# Path to SSH keys on host machine
# These will be mounted read-only into the container
# Default: ~/.ssh (your user's SSH directory)
SSH_KEY_PATH=~/.ssh

# =============================================================================
# Optional: Notification Configuration
# =============================================================================
# These can also be set in config.toml

# Gotify notification server
# GOTIFY_URL=http://gotify:8080/message
# GOTIFY_TOKEN=your-gotify-token-here

# ntfy.sh notification service
# NTFY_URL=https://ntfy.sh
# NTFY_TOPIC=svrctlrs-alerts

# =============================================================================
# Optional: Plugin Configuration
# =============================================================================
# These can also be set in config.toml

# Enable/disable plugins
# PLUGIN_DOCKER_ENABLED=true
# PLUGIN_UPDATES_ENABLED=true
# PLUGIN_HEALTH_ENABLED=true
# PLUGIN_WEATHER_ENABLED=false
# PLUGIN_SPEEDTEST_ENABLED=false

# Weather plugin (OpenWeatherMap)
# WEATHER_API_KEY=your-openweathermap-api-key
# WEATHER_LOCATION=New York,US

# =============================================================================
# Optional: Advanced Configuration
# =============================================================================

# Custom config file path (if not using default)
# CONFIG_PATH=/app/config.toml

# Data directory (internal to container)
# DATA_DIR=/app/data

# =============================================================================
# Docker Compose Options
# =============================================================================

# Compose project name (optional)
# COMPOSE_PROJECT_NAME=svrctlrs

# Container restart policy
# RESTART_POLICY=unless-stopped

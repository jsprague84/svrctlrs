<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.svrctlrs_desktop" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Edge-to-edge: draw behind status bar and navigation bar -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">false</item>
        <!-- Dark background to match app theme -->
        <item name="android:colorBackground">#1a1b26</item>
    </style>
</resources>

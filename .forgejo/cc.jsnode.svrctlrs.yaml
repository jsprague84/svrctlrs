app-id: cc.jsnode.svrctlrs
runtime: org.gnome.Platform
runtime-version: '46'
sdk: org.gnome.Sdk
command: svrctlrs-desktop

finish-args:
  # Network access (SSH connections, API calls)
  - --share=network
  # Display
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  # GPU rendering (WebGL for xterm.js)
  - --device=dri
  # SSH keys access (read-only)
  - --filesystem=~/.ssh:ro

modules:
  - name: svrctlrs
    buildsystem: simple
    build-commands:
      - install -Dm755 svrctlrs-desktop /app/bin/svrctlrs-desktop
      - install -Dm644 cc.jsnode.svrctlrs.desktop /app/share/applications/cc.jsnode.svrctlrs.desktop
      - install -Dm644 icon.png /app/share/icons/hicolor/256x256/apps/cc.jsnode.svrctlrs.png
    sources:
      - type: file
        path: svrctlrs-desktop
      - type: file
        path: cc.jsnode.svrctlrs.desktop
      - type: file
        path: icon.png

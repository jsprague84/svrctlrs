# Ralph Progress Log — Phase 4: Tauri Mobile
Started: Fri Mar 21 2026

## Codebase Patterns
- Branch: ralph/phase4-tauri-mobile (from ralph/phase3-tauri-desktop)
- IMPORTANT: Set env vars before ALL tauri commands:
  export JAVA_HOME=/usr/lib/jvm/java-21-openjdk
  export ANDROID_HOME=$HOME/Android/Sdk
  export NDK_HOME=$ANDROID_HOME/ndk/30.0.14904198
  export PATH=$PATH:$ANDROID_HOME/platform-tools:$ANDROID_HOME/cmdline-tools/latest/bin
- Tauri project: ui/src-tauri/ (excluded from root Cargo workspace)
- Android generated project: ui/src-tauri/gen/android/
- Platform detection: ui/src/lib/platform/index.ts (isWeb, isTauri, isTauriMobile, etc.)
- Keyboard detection: ui/src/lib/platform/keyboard.ts (web fallback via visualViewport)
- API client uses getServerUrl() for Tauri mode backend URL
- Desktop build: `cd ui && npx tauri build` → binary + .deb + .rpm
- Android build: `cd ui && npx tauri android build --debug` → APK
- Frontend quality: `cd ui && npm run check && npm run build`
- Backend quality: `cargo clippy --workspace -- -D warnings && cargo test --workspace`
- Rust Android targets installed: aarch64-linux-android, armv7-linux-androideabi, x86_64-linux-android, i686-linux-android
- Android SDK: API 36.1, NDK 30.0.14904198, Java 21

---

## 2026-03-21 - US-001: Android target init
- `tauri android init` generates src-tauri/gen/android/
- Must gate tray imports with #[cfg(desktop)] — mobile build fails on tray-icon types
- **KEY LEARNING:** Fedora system OpenJDK breaks Gradle toolchain detection. Must use
  Android Studio's bundled JBR at ~/Development/android-studio/jbr
- Debug APK: 388MB (universal, all 4 archs + debug symbols)
- Set JAVA_HOME to JBR path in gradle.properties AND env

## 2026-03-21 - US-002/003/004: Plugins + biometrics + config
- @tauri-apps/plugin-biometric exists officially (2.3.2) — installed + configured
- No dedicated keyboard plugin — web visualViewport heuristic sufficient
- @tauri-apps/plugin-os for platform detection
- Plugins registered in Rust code + capabilities/default.json
- biometrics.ts: dynamic imports with graceful web fallback (no-op)

## 2026-03-21 - US-005: Integration testing
- All backend (clippy + 18 tests) and frontend (check + build) pass
- CLAUDE.md updated with mobile build commands
---

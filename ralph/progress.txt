# Ralph Progress Log
Started: Thu Mar 20 2026

## Codebase Patterns
- Branch: ralph/phase1-foundation (from ralph/code-quality-audit) — terminal/SSH management tool
- IMPORTANT: main branch is stale (old job system). Never use as base.
- Rust workspace: core, database, server (scheduler exists but unused)
- SvelteKit 5 frontend in `ui/` — Svelte 5 runes ($state, $derived, $effect), Tailwind CSS v4, adapter-static
- State modules in `ui/src/lib/state/*.svelte.ts` export getter functions wrapping `$state` variables
- Database queries in `database/src/queries/` — use `anyhow::Context` for errors, `sqlx::QueryBuilder` for dynamic SQL
- Credential values encrypted at rest with AES-256-GCM via `core/src/encryption.rs`
- Two SSH libraries: `async-ssh2-tokio` (non-interactive, terminal.rs) and `russh` (PTY/interactive, terminal_pty.rs)
- ssh.rs uses NoCheck for host keys (DEPRECATED), terminal.rs implements proper TOFU
- Backend quality checks: `cargo clippy --workspace -- -D warnings && cargo test --workspace`
- Frontend quality checks: `cd ui && npm run check && npm run build`
- Svelte 5 uses callback props (onSomething), NOT createEventDispatcher (Svelte 4)
- API routes registered in `server/src/routes/api/mod.rs` routes() function, NOT in main.rs
- Use Context7 MCP tool to research latest library patterns before implementing
- Use /simplify skill after completing implementation for code quality review
- Icon library: lucide-svelte
- Mobile breakpoint: md (768px), detection via `ui/src/lib/state/mobile.svelte.ts`
- Error handling: use `extractErrorMessage()` from `$lib/utils/error.js` in all catch blocks
- Design spec: docs/superpowers/specs/2026-03-20-svrctlrs-next-phase-design.md
- Implementation plan: docs/superpowers/plans/2026-03-20-phase1-foundation.md

---

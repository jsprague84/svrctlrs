# Ralph Progress Log — Phase 2: Mobile UX
Started: Fri Mar 21 2026

## Codebase Patterns
- Branch: ralph/phase2-mobile-ux (from ralph/phase1-foundation)
- IMPORTANT: main branch is stale (old job system). Never use as base.
- Rust workspace: core, database, server (scheduler exists but unused)
- SvelteKit 5 frontend in `ui/` — Svelte 5 runes, Tailwind CSS v4, adapter-static
- State modules: `ui/src/lib/state/*.svelte.ts` — $state with exported getter functions
- Error handling: use `extractErrorMessage()` from `$lib/utils/error.js`
- Svelte 5: callback props (onSomething), NOT createEventDispatcher
- API routes registered in `server/src/routes/api/mod.rs` routes(), NOT main.rs
- Use sqlx::QueryBuilder for dynamic SQL, anyhow::Context for errors
- Mobile breakpoint: md (768px), detection via `isMobile()` from `$lib/state/mobile.svelte.js`
- viewport-fit=cover already set in app.html for safe-area support
- touch-action: manipulation already set (no double-tap zoom)
- Sidebar already uses safe-area-inset-left, TerminalPrefsPanel uses safe-area-inset-bottom
- Terminal serialization: @xterm/addon-serialize saves to sessionStorage on disconnect/visibility change
- Command palette: Ctrl+K (desktop) / double-tap (mobile), CommandPalette.svelte
- Profiles: ProfileManager.svelte (save), Sidebar (load/delete), profiles.svelte.ts (state)
- Backend quality: `cargo clippy --workspace -- -D warnings && cargo test --workspace`
- Frontend quality: `cd ui && npm run check && npm run build`
- Use Context7 MCP tool to research latest library patterns before implementing
- Use /simplify skill after completing implementation for code quality review
- Design spec: docs/superpowers/specs/2026-03-20-svrctlrs-next-phase-design.md

---

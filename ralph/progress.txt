# Ralph Progress Log — Phase 6: Advanced Features
Started: TBD (waiting for Phase 1-5 testing)

## Codebase Patterns
- Branch: ralph/phase6-advanced (from ralph/phase5-rstify)
- Tauri IPC: #[tauri::command] async functions, called via invoke() from JS
- Tauri events: app.emit() from Rust, listen() from JS — for streaming data
- russh for SSH: Client::connect(), session.channel_open_session(), channel.exec()/request_shell()
- Quick commands in quick_commands table — webhook allowlist
- NotificationClient in core/src/notifications.rs — Gotify/ntfy compatible
- Health monitor in server/src/health_monitor.rs — background tokio task
- Tauri env: JAVA_HOME=~/Development/android-studio/jbr, ANDROID_HOME, NDK_HOME
- Set env vars before ALL tauri commands
- Backend quality: `cargo clippy --workspace -- -D warnings && cargo test --workspace`
- Frontend quality: `cd ui && npm run check && npm run build`

---

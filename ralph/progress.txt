# Ralph Progress Log — UI Polish
Started: Fri Mar 21 2026

## Codebase Patterns
- Branch: ralph/ui-polish (from ralph/phase5-rstify)
- Settings stored as key-value pairs in `settings` table
- settingsState: getSettings() returns grouped by prefix, updateSetting(key, {value})
- quickCommandsState: loadCommands(), createCommand(), updateCommand(), deleteCommand()
- profilesState: getProfiles(), createProfile(), updateProfile(), deleteProfile()
- serversState: getServers() for server dropdown data
- Svelte 5 callback props, NOT dispatch
- Use extractErrorMessage() from $lib/utils/error.js in all catch blocks
- isMobile() from $lib/state/mobile.svelte.js for responsive
- isTauri(), getServerUrl(), getPlatform() from $lib/platform/index.js
- Shared UI components: Modal, Button, Input, Select, Badge, Toast
- Frontend quality: `cd ui && npm run check && npm run build`
- Backend quality: `cargo clippy --workspace -- -D warnings && cargo test --workspace`

---

# Ralph Progress: SvrCtlRS Terminal-First Optimization
# Branch: ralph/terminal-first-optimization
# Started: 2026-02-28

## Codebase Patterns
- Database models go in `database/src/models/<name>.rs`, registered in `database/src/models/mod.rs`
- Database queries go in `database/src/queries/<name>.rs`, registered in `database/src/queries/mod.rs`
- Both mod.rs files use `pub mod` + `pub use` glob re-exports
- Query functions follow pattern: `#[instrument(skip(pool))]`, return `Result<T>`, use `anyhow::Context` with `.map_err(|e| Error::DatabaseError(e.to_string()))`
- Tests use `setup_test_db()` with `:memory:` SQLite pool + `sqlx::migrate!().run(&pool)`
- Migrations are numbered sequentially (currently at 017)
- argon2 v0.5 uses `password_hash` crate traits: `PasswordHasher`, `PasswordVerifier`, `PasswordHash`
- tower-sessions version compatibility: use `tower-sessions = "0.14"` with `tower-sessions-sqlx-store = "0.15"` — they share `tower-sessions-core 0.14.0`. Other version combos cause `SessionStore` trait mismatch.
- `ExpiredDeletion` trait must be imported (`use tower_sessions::ExpiredDeletion`) to call `continuously_delete_expired()` on session stores
- AppError variants: `DatabaseError`, `TemplateError`, `NotFound`, `ValidationError`, `InternalError` (defined in `server/src/routes/ui/mod.rs`)
- Session key constant pattern: `const USER_ID_KEY: &str = "user_id"` in auth.rs for session get/insert
- Auth middleware pattern: use `axum::middleware::from_fn` with `Session` extractor. Layer must be placed INSIDE (added before) session layer so Session extractor works. Order: `routes.layer(auth_middleware).layer(session_layer)`.
- Exempt path checking in middleware: check `request.uri().path()` before session extraction for performance on static assets
- WebSocket detection in middleware: check `request.headers().get("upgrade")` for "websocket" value (case-insensitive)
- Encryption uses `OnceLock<[u8; 32]>` global for the key — initialized once at startup via `encryption::init_encryption_key()`, then `encryption::is_initialized()` / `encrypt()` / `decrypt()` used in query layer
- Credential queries transparently encrypt/decrypt using the global key; if key not initialized, stores plaintext with `encrypted = false` flag
- AES-256-GCM format: base64(nonce_12bytes || ciphertext || auth_tag) — nonce is random per encryption
- Terminal page route lives in `server/src/routes/ui/debug.rs` (registered as `/terminal`), template is `server/templates/pages/terminal.html`
- `TerminalPageTemplate` struct in `server/src/templates.rs` defines the Askama template data
- `TerminalManager` class in `server/static/js/terminal.js` is the shared xterm.js wrapper — instantiate separate instances for full-page vs modal usage
- Server list for terminal sidebar: use `queries::servers::list_servers_with_details()` → filter `.enabled` → map to `ServerDisplay`
- Terminal has two modes: CMD (`/ws/terminal`) for one-shot command execution, PTY (`/ws/terminal/pty`) for interactive shell via russh. Both use the same `TerminalManager` class but different connect methods (`connect()` vs `connectPty()`).

---

## 2026-02-28 - US-001
- Implemented users table and argon2 password hashing
- Files created:
  - `database/migrations/016_user_accounts.sql` - users table with id, username (UNIQUE), password_hash, timestamps
  - `database/src/models/user.rs` - User and CreateUser structs
  - `database/src/queries/users.rs` - create_user, get_user_by_username, verify_password, count_users + 5 tests
- Files modified:
  - `core/Cargo.toml` - added argon2 v0.5
  - `database/Cargo.toml` - added argon2 v0.5
  - `database/src/models/mod.rs` - registered user module
  - `database/src/queries/mod.rs` - registered users module
  - `Cargo.lock` - updated
- **Learnings for future iterations:**
  - argon2 crate uses `password_hash` sub-crate for OsRng, SaltString, PasswordHash, PasswordHasher, PasswordVerifier traits
  - `verify_password` is a sync function (no async needed) - argon2 verification is CPU-bound
  - CreateUser takes plaintext password; hashing happens in the query layer (create_user function)
  - count_users() helper added for checking if any users exist (useful for initial admin seeding in US-002)
---

## 2026-02-28 - US-002
- Implemented login page and session management
- Files modified:
  - `server/Cargo.toml` - upgraded tower-sessions to 0.14, tower-sessions-sqlx-store to 0.15, added `time` crate
  - `server/src/main.rs` - integrated SqliteStore session store with migration, expired session cleanup task, SessionManagerLayer middleware, admin user seeding from ADMIN_USERNAME/ADMIN_PASSWORD env vars
  - `server/src/routes/ui/auth.rs` - login handler now verifies password via `users::verify_password`, stores `user_id` in session; logout handler flushes session; error renders login page with message
  - `server/src/templates.rs` - removed `#[allow(dead_code)]` from LoginForm fields (now used)
  - `Cargo.lock` - updated dependency graph
- **Learnings for future iterations:**
  - tower-sessions ecosystem has persistent version misalignment: `tower-sessions-sqlx-store` always lags one `tower-sessions-core` version behind `tower-sessions`. Must pair carefully (0.14 + 0.15 works).
  - `Session` extractor from tower-sessions only works as Axum extractor when the correct `axum-core` feature version is compatible (tower-sessions 0.13+ for axum 0.8)
  - `session.flush()` destroys the session (for logout); `session.insert(key, value)` and `session.get::<T>(key)` for data storage
  - Admin seeding uses `count_users()` from US-001 to check if any users exist before creating from env vars
  - Login error handling: return `Html(template.render()?).into_response()` for error, `Redirect::to("/").into_response()` for success (both need `.into_response()` to unify return type)
---

## 2026-02-28 - US-003
- Implemented authentication middleware to protect all routes
- Files modified:
  - `server/src/routes/ui/auth.rs` - Added `require_auth` middleware function using `axum::middleware::from_fn`
  - `server/src/main.rs` - Applied auth middleware layer between routes and session layer
- Implementation details:
  - Middleware checks `Session` for `user_id` key; if present, request proceeds
  - Exempt routes: `/auth/login`, `/auth/logout`, `/static/*`, `/api/v1/health`
  - Unauthenticated WebSocket upgrades → 401 (checks `Upgrade: websocket` header)
  - Unauthenticated API requests (`/api/*`) → 401 JSON `{"error": "Authentication required"}`
  - Unauthenticated browser requests → 302 redirect to `/auth/login`
- **Learnings for future iterations:**
  - Axum middleware layer ordering is critical: `.layer(auth).layer(session)` means session processes FIRST (outermost), then auth runs with session available
  - `Session` implements `FromRequestParts` so it can be used as an extractor parameter in `from_fn` middleware
  - `is_some_and()` is a clean Rust idiom for checking Option contents with a predicate
  - The health endpoint is at `/api/v1/health` (nested under `/api` then `/v1`), not `/api/health`
---

## 2026-02-28 - US-004
- Implemented AES-256-GCM credential encryption at rest
- Files created:
  - `core/src/encryption.rs` - AES-256-GCM encrypt/decrypt with OnceLock global key, key loading from ENCRYPTION_KEY env var or data/encryption.key file, auto-generation with 0600 permissions (~200 lines, 10 tests)
  - `database/migrations/017_credential_encryption_flag.sql` - adds `encrypted` BOOLEAN DEFAULT 0 to credentials table
- Files modified:
  - `core/Cargo.toml` - added aes-gcm 0.10, rand 0.8, base64 0.22, hex 0.4
  - `core/src/lib.rs` - registered `pub mod encryption`
  - `database/src/models/credential.rs` - added `encrypted: bool` field to Credential struct
  - `database/src/queries/credentials.rs` - create_credential encrypts value before INSERT, get_credential/get_credential_by_name/list_credentials decrypt after SELECT, update_credential encrypts value if changed
  - `server/src/main.rs` - added `encryption::init_encryption_key()` call at startup (graceful fallback if fails)
  - `Cargo.lock` - updated dependency graph
- **Learnings for future iterations:**
  - `OnceLock` from std is ideal for one-time initialization of encryption keys — avoids threading key through every query function
  - AES-256-GCM from `aes-gcm` crate: `Aes256Gcm::new(key.into())` for cipher, `cipher.encrypt(nonce, plaintext)` / `cipher.decrypt(nonce, ciphertext)` for operations
  - `rand::RngCore::fill_bytes(&mut OsRng, &mut buf)` for cryptographically secure random nonce generation
  - Transparent encryption in query layer means zero changes to ~12 call sites across the server — decrypt_credential() helper applied to all fetch paths
  - Added `encrypted` column flag so existing plaintext credentials remain readable while new ones are encrypted
  - Key format: 64 hex chars = 32 bytes. Stored in env var or `data/encryption.key` file
  - Test suite works without encryption initialized — queries gracefully fall back to plaintext storage
---

## 2026-02-28 - US-005
- Redesigned terminal page as full-page terminal with collapsible server sidebar
- Files modified:
  - `server/templates/pages/terminal.html` - Complete rewrite: replaced multi-terminal debug layout with full-viewport terminal + collapsible server sidebar (~250px) + command input bar
  - `server/src/routes/ui/debug.rs` - Simplified `terminal_page` handler to only load enabled servers (removed tags and profiles loading)
  - `server/src/templates.rs` - Removed `tags` and `profiles` fields from `TerminalPageTemplate` struct (no longer needed by template)
  - `ralph/prd.json` - Marked US-005 as passing
- Implementation details:
  - Left sidebar lists all enabled servers with name, hostname:port, and Connect button
  - Sidebar is collapsible with expand/collapse toggle buttons
  - Main area: full-viewport xterm.js terminal using dedicated `TerminalManager` instance
  - Top bar shows connected server name, status badges, and action buttons (clear, copy, download, disconnect)
  - Bottom command input bar with textarea (Shift+Enter for multi-line), command history (up/down arrows)
  - `fullPageTerminal()` Alpine.js component manages state: sidebar toggle, server connection, command execution
  - Terminal auto-fits on window resize and sidebar toggle via `fitTerminal()` calls
  - Auto-connect from URL: reads `?server_id=` query parameter on load
  - Uses separate `TerminalManager` instance from the global modal one to avoid conflicts
  - Terminal link already existed in base.html sidebar navigation under "Tools" section
- **Learnings for future iterations:**
  - The existing `/terminal` route was a complex multi-pane debug page — redesigning it requires updating both template and Askama struct fields
  - `TerminalManager` instances are independent — create new ones for different contexts (modal vs full-page) to avoid shared state issues
  - Use `$nextTick()` + `setTimeout()` for terminal `fit()` after DOM layout changes (sidebar toggle, modal open)
  - Override `.main-content` padding with `!important` in page-specific styles to get true full-viewport layout
  - `TerminalProfileDisplay` struct in templates.rs becomes dead code when removed from template — `#![allow(dead_code)]` at module level covers it
---

## 2026-02-28 - US-006
- Integrated PTY mode toggle in the full-page terminal frontend
- Files modified:
  - `server/templates/pages/terminal.html` - Added mode toggle UI, mode badge, PTY session support, command bar visibility toggle
  - `ralph/prd.json` - Marked US-006 as passing
- Implementation details:
  - Mode toggle in topbar: "Command" (CMD) and "Shell" (PTY) buttons with active state highlighting
  - Mode badge in topbar header shows "CMD" (blue) or "PTY" (green) indicator
  - Command mode: existing behavior — type command in input bar, see output in terminal
  - Shell mode: hides command bar, starts interactive PTY session via `/ws/terminal/pty`, all keyboard input forwarded directly to PTY
  - Switching modes disconnects current WebSocket and connects to the appropriate endpoint
  - `connectToServer()` is mode-aware: CMD mode selects server + shows welcome, PTY mode immediately starts interactive shell
  - `switchMode()` handles mid-session mode changes with auto-reconnect to new endpoint
  - Terminal auto-fits when mode changes (command bar show/hide changes terminal height)
  - No backend changes needed — existing `terminal.rs` (CMD) and `terminal_pty.rs` (PTY) endpoints and `TerminalManager.connectPty()` method already fully implemented
- **Learnings for future iterations:**
  - `TerminalManager` already had `connectPty()` and `ptyMode` support from the modal component — reuse in full-page just means calling the right method based on mode state
  - Alpine.js `x-show` with mode state cleanly hides/shows the command bar without layout jumps (flex layout recalculates automatically)
  - Watch `mode` changes and call `fitTerminal()` after `$nextTick` to ensure terminal resizes when command bar appears/disappears
  - `disconnectTerminal()` should also reset `executing` state to prevent stale UI after mode switches
---

## 2026-02-28 - US-007
- Added WebGL renderer to xterm.js terminal (mostly pre-existing, minor fix)
- Files modified:
  - `server/static/js/terminal.js` - Fixed onContextLoss retry delay from 2000ms to 1000ms per AC
  - `ralph/prd.json` - Marked US-007 as passing
- Pre-existing implementation verified:
  - `@xterm/addon-webgl@0.18.0` already loaded from CDN in `base.html` (line 70)
  - `loadWebGLAddon()` method already exists (lines 152-176), called after `terminal.open()` (line 131)
  - `onContextLoss` handler disposes addon and retries (now at 1s delay)
  - Canvas fallback with console warning on WebGL failure
  - Unicode11 addon with `activeVersion = '11'` already set (line 98)
- **Learnings for future iterations:**
  - WebGL addon, Unicode11, Clipboard, Image, and Serialize addons were all pre-loaded in prior iterations — check existing code before implementing "new" features
  - `loadWebGLAddon()` must be called AFTER `terminal.open()` — WebGL needs an attached canvas element
  - Context loss recovery pattern: dispose → nullify → setTimeout → retry load
---

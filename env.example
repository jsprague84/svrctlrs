# ============================================
# SvrCtlRS Environment Configuration
# ============================================
# Copy this file to .env and customize as needed
#
# NOTE: All application configuration (plugins, notifications, servers)
# is now managed through the database and UI at http://localhost:8080
#
# This file contains ONLY infrastructure settings!
# ============================================

# ============================================
# Docker Image
# ============================================
GITHUB_REPOSITORY=jsprague84/svrctlrs
TAG=develop

# ============================================
# Server Configuration
# ============================================
HTTP_PORT=8080
RESTART_POLICY=unless-stopped

# ============================================
# Logging
# ============================================
# Options: trace, debug, info, warn, error
RUST_LOG=info

# ============================================
# Database
# ============================================
# Path to SQLite database file inside container
DATABASE_URL=sqlite:/app/data/svrctlrs.db

# ============================================
# SSH Configuration (Optional)
# ============================================
# Path to SSH keys DIRECTORY (not a single key file!)
# The container will automatically try:
#   - id_ed25519
#   - id_rsa
#   - id_ecdsa
#   - id_dsa
#
# Default: ~/.ssh (your user's SSH directory)
# SSH_KEY_PATH=~/.ssh

# ============================================
# Docker Plugin Configuration (Auto-detected)
# ============================================
# The Docker plugin requires the container user to have access to
# the Docker socket. This is done by setting the user's GID to match
# the docker group on the host.
#
# DOCKER_GID is auto-detected when you start the container.
# If auto-detection fails, you can manually set it here.
#
# To find your docker group GID:
#   stat -c '%g' /var/run/docker.sock
#
# Then uncomment and set:
# DOCKER_GID=999
#
# If you don't need Docker monitoring, comment out the Docker socket
# mount in docker-compose.yml for better security.

# ============================================
# REMOVED - Now Configured via UI
# ============================================
# The following settings are NO LONGER in environment variables.
# Configure them through the web UI instead:
#
# ❌ ENABLE_*_PLUGIN=*        → Configure at /plugins
# ❌ GOTIFY_URL=*             → Configure at /settings/notifications
# ❌ GOTIFY_TOKEN=*           → Configure at /settings/notifications
# ❌ NTFY_URL=*               → Configure at /settings/notifications
# ❌ NTFY_TOPIC=*             → Configure at /settings/notifications
# ❌ WEATHER_API_KEY=*        → Configure at /plugins (Weather plugin)
# ❌ WEATHER_LOCATION=*       → Configure at /plugins (Weather plugin)
#
# All application configuration is now in the database!
# Access the UI at: http://localhost:8080
# ============================================

# ============================================
# Quick Start
# ============================================
# 1. Copy this file:
#      cp env.example .env
#
# 2. Start the container:
#      docker compose up -d
#
# 3. Access the UI:
#      http://localhost:8080
#
# 4. Configure everything through the UI:
#      - Plugins: /plugins
#      - Notifications: /settings/notifications
#      - Servers: /servers
#      - Tasks: /tasks
#
# That's it! No more environment variable configuration needed.
# ============================================
